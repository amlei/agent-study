{
  "inputs": [
    {
      "type": "promptString",
      "id": "notion-api-key",
      "description": "Notion API Key",
      "password": true
    }
  ],
  "servers": {
    "notion": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_API_KEY": "${input:notion-api-key}"
      }
    }
}
}